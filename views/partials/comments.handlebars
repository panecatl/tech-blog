<div class="comments">
    {{#each this}}
    <div class="card">
        <h3 class="card-header"></h3>
        <section class="comment card-body">
            <div class="text">
                {{coment_text}}
            </div>
            <div class="meta">
                {{user.username}}, {{format_date created_at}}
            </div>
        </section>
    </div>
    {{/each}}
</div>